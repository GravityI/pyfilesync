Requirements:

    - Command Line Interface that asks for source and replica directory paths, synchronization interval and log file path.
    - Periodically create/copy/remove files on the replica directory according to the interval so that its contents are identical to those of the source directory.
    - Log every create/copy/remove operation in the CLI as well as in a log file on the specified path.
